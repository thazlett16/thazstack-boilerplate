// DateTimePicker reuses the same visual structure as DatePicker.
// Re-export so consumers can override specific slots if needed.
export { datePickerVariants as dateTimePickerVariants } from '#src/common/components/date-picker/variants';
export type { DatePickerVariants as DateTimePickerVariants } from '#src/common/components/date-picker/variants';
