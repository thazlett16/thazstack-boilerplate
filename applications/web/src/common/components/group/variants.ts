import { tv } from 'tailwind-variants';

export const groupVariants = tv({
  base: [
    'group/group',
    // 'data-disabled:',
    // 'data-hovered:',
    // 'data-invalid:',
    // 'data-focus-visible:',
    // 'data-focus-within:',
  ],
});

// export type GroupVariants = VariantProps<typeof groupVariants>;
// export type SlotsGroupVariants = ReturnType<typeof groupVariants>;
// export type RequiredGroupVariants = Required<SetNonNullable<GroupVariants>>;
